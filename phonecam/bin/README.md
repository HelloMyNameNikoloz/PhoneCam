# Runtime Binaries

Place the Windows scrcpy package files here before running or packaging:

- `adb.exe`
- `AdbWinApi.dll`
- `AdbWinUsbApi.dll`
- `scrcpy.exe`

PhoneCam handles missing files gracefully, but camera streaming requires these binaries.
